{
  "name": "@prismark/theme",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens from DESIGN.md, emitted as one Tailwind theme.css for web and native.",
  "type": "module",
  "exports": {
    ".": "./src/tokens.ts",
    "./theme.css": "./dist/theme.css"
  },
  "scripts": {
    "build": "node build.ts",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@prismark/config": "workspace:*",
    "@types/node": "^22.20.1",
    "typescript": "^6.0.2"
  }
}
