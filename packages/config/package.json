{
  "name": "@prismark/config",
  "version": "0.0.0",
  "private": true,
  "description": "Baseline configs shared across the monorepo.",
  "files": [
    "oxlint.json",
    "oxlint-react.json"
  ]
}
